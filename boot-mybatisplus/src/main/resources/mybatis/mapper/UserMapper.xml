<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.loserico.bootmybatisplus.mapper.UserMapper">
    
    <select id="queryById" parameterType="long" resultType="com.loserico.bootmybatisplus.entity.User">
        select * from t_user where id=#{id}
    </select>
</mapper>